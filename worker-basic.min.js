/**
 * @license
 * Copyright Google LLC All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file at https://angular.io/license
 */

// tslint:disable:no-console

self.addEventListener('install', event => {
  self.skipWaiting();
});

self.addEventListener('activate', event => {
  event.waitUntil(self.clients.claim());

  event.waitUntil(self.registration.unregister().then(() => {
    console.log('NGSW Safety Worker - unregistered old service worker');
  }));

  event.waitUntil(caches.keys().then(cacheNames => {
    const ngswCacheNames = cacheNames.filter(name => /^ngsw:/.test(name));
    return Promise.all(ngswCacheNames.map(name => caches.delete(name)));
  }));
});

//Comment for commit 1
//Comment for commit 2
//Comment for commit 3
//Comment for commit 4
//Comment for commit 5
//Comment for commit 6
//Comment for commit 7
//Comment for commit 8
//Comment for commit 9
//Comment for commit 10
//Comment for commit 11
//Comment for commit 12
//Comment for commit 13
